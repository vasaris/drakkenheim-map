assets/*
!assets/.gitkeep
*.pdf
*.jpg
*.jpeg
*.png
*.webp
!js/vendor/leaflet/images/*.png
.DS_Store

# tile pyramid: served from a local symlink, never committed (audited — *.png alone
# does not catch the symlink itself)
tiles
tiles/

# Ф3.3-Т: Playwright test harness
node_modules/
test-results/
playwright-report/
artifacts/

# Ф3.5а: pre-rotation backups from scripts/rotate-passphrase.mjs — local safety net only
# (Ф5: rotate-passphrase.mjs repointed data/v2 -> data/v3, see script header)
data/v3/*.bak

# Ф3.5б: lifecycle e2e fixture — generated fresh from data/v3 by tests/global-setup.js
# (Ф3.5в: source switched from data/v2 to data/v3) before every test run (fixture
# password only); committing a snapshot is exactly the
# staleness bug this replaced (screenshot/test not reflecting a data edit)
tests/fixtures/lifecycle-zones.json
tests/fixtures/lifecycle-markers.json
